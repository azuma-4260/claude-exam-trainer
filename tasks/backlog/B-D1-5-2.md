---
id: B-D1-5-2
origin: D1-5
created: 2026-08-27
status: open
related_tasks: [D5-1]
related_specs: ["04#直前期920926と-d-1", "05#s-1-home"]
related_paths: [src/lib/queue/serve.ts, src/lib/queue/build.ts]
related_backlog: [B-T-queue-1]
stop_condition: none
---
# D-1(9/26)はセレクタ未実装のため Home / Drill が案内表示のみになる

## 内容

`buildDailyQueue` は D-1 モードで `selectDMinus1` 未提供なら throw する(fail closed、
通常キューへ fallback しない)。D1-5 の `assembleQueueView` は throw に到達させず
`kind: "d_minus_1_unavailable"` を返し、Home / Drill は「前日メニューは準備中(D5-1)」の
案内を表示する。**D-1 の「間違いノート → low-stability 順」の実装は D5-1 の責務**。

## 再現手順 / 根拠

`src/lib/queue/serve.test.ts` の「D-1(試験前日)はセレクタ未実装のため
d_minus_1_unavailable」テスト。now = 2026-09-26 で再現。

## 推奨対応

D5-1 でセレクタを実装し、`assembleQueueView` の D-1 分岐へ注入する
(`queueModeFor` 分岐を selectDMinus1 提供時に buildDailyQueue へ通すだけ)。
9/26 までに D5-1 が完了しない場合は当日キューが配信されないことをオーナーが了解しておく。
